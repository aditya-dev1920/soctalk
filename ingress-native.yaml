apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: soctalk-ui-ingress-9080
  namespace: soctalk-system
spec:
  entryPoints:
    - custom9080
  routes:
    - match: PathPrefix(`/`)
      kind: Rule
      services:
        - name: soctalk-system-app-ui
          port: 3000
